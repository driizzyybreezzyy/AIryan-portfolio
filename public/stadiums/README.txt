Drop real stadium photos here to replace the fallback gradients.
Use these exact filenames (landscape, ~1600x900 or larger):

  anfield.jpeg       — Liverpool         (hero / top)
  bernabeu.jpeg      — Real Madrid       (Profile)
  campnou.jpeg       — Barcelona         (Career)
  allianz.jpeg       — Bayern Munchen    (Highlights)
  etihad.jpeg        — Manchester City   (Honours)
  dortmund.jpeg      — Signal Iduna Park (Attributes)
  oldtrafford.jpeg   — Manchester United (Sign me)

These files are already present. To change which ground maps to which
section, edit the GROUNDS list in src/components/StadiumBackground.tsx
(the `img` path must match the filename here exactly).

Note: real stadium photos are copyrighted. Use images you have the right to use.
Until a file is added, that section shows a club-coloured, pitch-striped gradient.
